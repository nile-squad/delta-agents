# ADR-006: Runtime Decision

> **Status:** Being repurposed for delta-agents. Previous @nilejs/future content is in git history.

This decision record should document the runtime choice for delta-agents. The previous ADR covered Bun-only for worker-thread actor isolation. Delta-agents may have different runtime requirements depending on its governance engine architecture. Review the spec's stateless governance engine decision and document the runtime implications.

See [delta-agents.spec.md](../delta-agents.spec.md) for the canonical specification.
