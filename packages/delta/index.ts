/**
 * delta-agents — Deterministic autonomous control plane for AI agents.
 *
 * The model is responsible for reasoning.
 * The engine is responsible for governance.
 *
 * See delta-agents.spec.md for the full specification.
 */

// Slang functional utilities (Result, Option, match, safeTry, pipe, etc.)
export * from "./src";
